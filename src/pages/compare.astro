---
import BaseLayout from "../layouts/BaseLayout.astro";
import ComparisonTable from "../components/ComparisonTable.vue";
import { getAllProjects, getMetadata } from "../lib/directory";

const projects = getAllProjects();
const metadata = getMetadata();
---

<BaseLayout title="Compare" description="Compare all agent harnesses side by side — sortable by name, category, stars, and features.">
  <section class="border-b border-black/10 bg-neutral-50 dark:border-white/10 dark:bg-neutral-900">
    <div class="mx-auto max-w-6xl px-6 py-16">
      <h1>Compare All Projects</h1>
      <p class="mt-4 max-w-2xl text-lg text-neutral-500">
        {metadata.total_entries} projects at a glance. Click any column header to sort.
      </p>
    </div>
  </section>

  <section>
    <div class="mx-auto max-w-6xl px-6 py-12">
      <ComparisonTable client:visible projects={projects} />
    </div>
  </section>
</BaseLayout>
